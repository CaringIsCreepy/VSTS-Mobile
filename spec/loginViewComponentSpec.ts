import {LoginView} from '../public/app/login/login-view.component';

describe("login.component", function() {
    it("should call getProfile()", function() {
        expect(true).toEqual(true);
    });
});